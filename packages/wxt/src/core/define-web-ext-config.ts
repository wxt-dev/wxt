import type { WxtRunnerConfig } from '../types';

/**
 * Configure how [`@wxt-dev/runner`](https://github.com/wxt-dev/wxt/tree/main/packages/runner) opens the browser during development.
 */
export function defineRunnerConfig(config: WxtRunnerConfig): WxtRunnerConfig {
  return config;
}
